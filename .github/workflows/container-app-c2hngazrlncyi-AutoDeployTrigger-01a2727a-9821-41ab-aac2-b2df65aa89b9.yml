name: Trigger auto deployment for container-app-c2hngazrlncyi

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-c2hngazrlncyi-AutoDeployTrigger-01a2727a-9821-41ab-aac2-b2df65aa89b9.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPC2HNGAZRLNCYI_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPC2HNGAZRLNCYI_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPC2HNGAZRLNCYI_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPC2HNGAZRLNCYI_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPC2HNGAZRLNCYI_REGISTRY_PASSWORD }}
          containerAppName: container-app-c2hngazrlncyi
          resourceGroup: expresssample91dd81f9
          imageToBuild: default/container-app-c2hngazrlncyi:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


